<Page
    x:Class="Gaming_U.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Gaming_U"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding PlayersViewModel, 
    RelativeSource={RelativeSource self}}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="{ThemeResource SystemAccentColor}"/>
    </Page.Resources>
    <Page.TopAppBar >
        <CommandBar Background="{StaticResource AccentBrush}">
            <CommandBar.SecondaryCommands>
                
            </CommandBar.SecondaryCommands>
            <CommandBar.Content>
                <Grid/>
            </CommandBar.Content>
            <AppBarButton x:ConnectionId='2'  Name="btnStartTimer" Visibility="Collapsed" Icon="Forward" Label="Start Timer"/>
            <AppBarButton x:ConnectionId='3' Name="btnStopTimer" Visibility="Collapsed" Icon="Pause" Label="Stop Timer"/>
            <AppBarButton x:ConnectionId='4' Name="btnClearTimer" Visibility="Collapsed" Icon="Clear" Label="Clear Timer"/>
            <AppBarButton x:ConnectionId='5' Name="btnTimerstart" Icon="Clock" Label="Show Timer"                       />

            <!--<AppBarButton Name="btnDiceroll" Icon="Rotate" Label="Dice" Click="btnDice_Click"/>-->

        </CommandBar>
    </Page.TopAppBar>

    <Page.BottomAppBar >
        <CommandBar Background="{StaticResource AccentBrush}" >

            <CommandBar.Content>
                <Grid/>
            </CommandBar.Content>
            <AppBarButton x:ConnectionId='6' Name="btnRemoveAll" Icon="Delete" Label="Reomve All"                           />
            <AppBarButton x:ConnectionId='7' Name="btnAdd" Icon="AddFriend" Label="Add Player"                     />
            <!--<AppBarButton Name="btnDice" Icon="Rotate" Label="Dice" Click="btnDice_Click"/>-->
            <!--<AppBarToggleButton Name="btnTeams" Icon="FontColor" Label="Team Play" Click="btnTeams_Click"/>-->
        </CommandBar>
    </Page.BottomAppBar>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" BorderBrush="Aquamarine"/>


        <StackPanel x:ConnectionId='8' 
            Name="stkTime"
            HorizontalAlignment="Stretch" 
            Grid.Column="0" Grid.ColumnSpan="2" Margin="15" Visibility="Collapsed">
            <TextBlock Text="Timer" Grid.Column="0"/>
            <Viewbox MaxHeight="95">
                <TextBlock x:ConnectionId='12' Text="00:00"  Name="txtTimer" 
                            Grid.Column="0"/>
            </Viewbox>

        </StackPanel>

        <!--<StackPanel Name="stkDice" Visibility="Collapsed" Grid.Column="1" HorizontalAlignment="Stretch" Margin="15">
            <TextBlock Text="Dice Roll" Grid.Column="1"/>

            <Viewbox MaxHeight="95">
                <TextBlock Text="0" Name="txtDice" Grid.Column="1" />
            </Viewbox>

        </StackPanel>-->

        <Border BorderBrush="Cyan" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
            <ListView x:ConnectionId='9' Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
            Name="lstofPlayers" 
                   Margin="25" 
                  HorizontalContentAlignment="Center" 
                  IsItemClickEnabled="True" 
                  IsMultiSelectCheckBoxEnabled="True"
                                                    
                  IsRightTapEnabled="True"
                                                                        
                                 
                  ItemsSource="{Binding}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"  />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:Name="dataTemplate">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>


                            <Viewbox Grid.Column="0" Stretch="Uniform" MaxHeight="250" MaxWidth="250">
                                <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Name="txtName"  
                                   Text="{Binding Name}"
                                   Margin="10,10,10,10"
                                   Padding="10"
                                FontWeight="SemiBold" 
                                TextAlignment="Center" 
                                FontSize="30" FontStyle="Oblique"
                                    HorizontalAlignment="Center"
                                    />

                                    <!--<TextBlock Name="txtID" 
                                   Text="{Binding Id}"
                                   Margin="0,10,10,0" Padding="10"
                                   FontWeight="Bold" 
                                TextAlignment="Center" 
                                FontSize="30" FontStyle="Oblique"
                                    HorizontalAlignment="Center"
                                    />-->
                                </StackPanel>
                            </Viewbox>
                            <Viewbox Grid.Column="1" Stretch="Uniform" MaxHeight="250" MaxWidth="250">
                                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button 
                                    Width="40" Height="40"
                                     Command="{Binding TakePointsCommand}"
                                     CommandParameter="{Binding}"
                                     Margin="10,10,10,10"
                                     FontFamily="Segoe MDL2 Assets" Content="&#xE108;"
                                     Background="{StaticResource AccentBrush}"/>

                                    <TextBlock Name="txtPoints" 
                                   Text="{Binding Amount}"
                                     Margin="10" Padding="10"
                                    FontWeight="SemiBold" 
                                    TextAlignment="Center" 
                                    FontSize="30" 
                                    HorizontalAlignment="Center"
                                              />

                                    <Button  
                                     Width="40" Height="40"
                                     Command="{Binding GivePointsCommand}"
                                     CommandParameter="{Binding}"
                                     Margin="10,10,10,10"
                                     FontFamily="Segoe MDL2 Assets" Content="&#xE109;"
                                   Background="{StaticResource AccentBrush}"/>

                                </StackPanel>
                            </Viewbox>
                            <!--The buttons to remove and clear the Points-->
                            <Viewbox Grid.Column="2" HorizontalAlignment="Center" Stretch="Uniform" MaxHeight="200" MaxWidth="200">
                                <StackPanel Grid.Column="2" HorizontalAlignment="Center" Orientation="Horizontal">
                                    <Button Name="btnClear" 
                                            Grid.Column="2" 
                                            HorizontalAlignment="Center"
                                             FontFamily="Segoe MDL2 Assets" Content="&#xE117;"
                                                Command="{Binding ClearPointsCommand}"
                                                CommandParameter="{Binding}"
                                                Margin="20"
                                                Width="40" Height="40"
                                            Background="{StaticResource AccentBrush}"/>
                                    <Button x:ConnectionId='11' Name="btnRemove" Grid.Column="2" HorizontalAlignment="Center"
                                          Background="{StaticResource AccentBrush}"
                                             FontFamily="Segoe MDL2 Assets" Content="&#xE107;"
                                             Width="40" Height="40"
                                                                   
                                             Margin="20"
                                    />
                                </StackPanel>
                            </Viewbox>



                            <!--<StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Name="stkChangeInfo" 
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Visibility="Visible">
                                <Viewbox>
                                    
                                    <TextBox Header="Points" Name="txtAmount"  Margin="15"/>
                                </Viewbox>
                                <Viewbox>
                                    <TextBox Header="Name" Name="txtNameChange"  Margin="15" />
                                </Viewbox>
                                
                            </StackPanel>-->
                        </Grid>



                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>
        </Border>
    </Grid>
</Page>

